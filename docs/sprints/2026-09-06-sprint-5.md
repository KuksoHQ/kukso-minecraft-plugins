# Sprint 5: 2026-09-06

## Sprint Goal

Remove confirmed-empty KuksoWorlds stubs and collapse `CmdRegistrar` so the module only advertises commands that actually register.

## Product Alignment

- Maintenance-only. Removes dead code and leftover default config/permission advertising so owners are not pointed at an unimplemented `/kuksoworlds reload` or unused role nodes.
- Aligns with the shipped-behavior documentation rule: config and `plugin.yml` describe live commands only. Real reload remains a backlog item.

## Dates

- **Start:** 2026-09-06
- **End:** 2026-09-13
- **Cadence:** 1 week

## Capacity And Assumptions

- One scoped area only: `:worlds` stubs plus registrar collapse.
- Do not touch dialogs orphan commands, lib lore stack, recycle-bin permissions, create/load WorldLoader behavior, or comment-only sweeps in files that do not need edits.
- `LoggingManager` stays: it has real methods and is used by `VersionCmd`.
- Prefer delete over rewrite. No new registration machinery.

## Committed Items

### Item 1: Delete Worlds Stubs And Collapse CmdRegistrar

- [ ] **Status:** In progress
- **Module/area:** `:worlds`
- **User value:** Server owners and maintainers are not shown empty unused classes, an unregistered reload command in default config, or unused role permission nodes.
- **Release target:** `Unreleased` (next `:worlds` release)
- **Changelog impact:** `Changed` and `Config And Permissions`
- **Acceptance criteria:**
  - [ ] Empty unused stubs deleted: `ReloadCmd`, `PvpHandling`, `modules.WorldBorder`, `hooks.ColorManager`, `hooks.VersionChecker`, `hooks.LocalizationManager`.
  - [ ] `LoggingManager` left in place unless proven empty and unused (it is used).
  - [ ] Default `config.yml` no longer advertises `commands.reload` / `kuksoworlds.command.reload`.
  - [ ] Unused `plugin.yml` nodes `kuksoworlds.admin` / `kuksoworlds.mod` / `kuksoworlds.user` removed if unreferenced.
  - [ ] `CmdRegistrar` collapsed to one register loop (or equivalent dialogs-style sequential registers) with no per-command try/catch wrappers, still registering the live commands.
  - [ ] No remaining references to deleted classes.
- **Validation:**
  - [ ] `./gradlew :worlds:build`
  - [ ] Grep for deleted class names is clean in `:worlds`.
  - [ ] `git diff --check`

## Work Log

| Date | Item | Update | Blockers/Decisions |
| --- | --- | --- | --- |
| 2026-09-06 | Item 1 | Verified the nightly-audit hypothesis. Empty stubs confirmed unregistered. `LoggingManager` is used by `VersionCmd` and stays. WorldLoader comments left alone (file not required for this kill). | Decision: kill-only plus registrar collapse. Real `/kuksoworlds reload` remains backlog. |

## Review

- **Completed items:** —
- **Verification:** —
- **Changelog status:** —
- **Not completed:** —

## Retro

- **Keep:** —
- **Change:** —
- **Follow up:** —

## Carryover

- [ ] Implement and register a real `/kuksoworlds reload` (existing backlog item).
