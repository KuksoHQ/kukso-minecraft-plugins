# Sprint 2: 2026-07-01

## Sprint Goal

Make the repository AI-first: encode the documented sprint and release workflows as Claude Code project skills, and close the safety gaps found by an AI-first audit.

## Product Alignment

- Maintenance-only sprint. It does not change plugin behavior; it makes future LLM-assisted sessions follow the existing `PRODUCT.md`-aligned workflows more reliably.

## Dates

- **Start:** 2026-07-01
- **End:** 2026-07-01
- **Cadence:** 1 week

## Capacity And Assumptions

- Documentation, repo hygiene, and CI cleanup only; no plugin behavior changes.
- Existing workflow docs (`docs/development-cycle.md`, `docs/releases/README.md`, sprint template) remain the source of truth; skills reference them instead of duplicating them.

## Committed Items

### Item 1: Encode Sprint And Release Workflows As Project Skills

- [x] **Status:** Done
- **Module/area:** repository workflow, `.claude/skills/`
- **User value:** Agent sessions run the sprint and release-prep workflows as one slash command with prescriptive steps, instead of rediscovering them from prose each session.
- **Release target:** `Not release-relevant`
- **Changelog impact:** `Added`
- **Acceptance criteria:**
  - [x] `.claude/skills/sprint/SKILL.md` covers backlog grooming, sprint file creation, work logging, and close-out, referencing `docs/sprints/` and `docs/development-cycle.md`.
  - [x] `.claude/skills/release-prep/SKILL.md` covers changelog moves, `version.txt` bump, validation, and the manual GitHub Release handoff, with tagging/publishing left to humans.
  - [x] `.claude/skills/release-prep/scripts/check-release-ready.sh` deterministically checks module, version format, `version.txt`, tag availability, and changelog state.
  - [x] Root `CLAUDE.md` imports `AGENTS.md` and points to the skills.
- **Validation:**
  - [x] `check-release-ready.sh worlds 9.9.9` exits 0 with READY; `check-release-ready.sh bogus 1.0` exits 1 with 3 failures.
  - [x] `git diff --check` clean; docs read-through.

### Item 2: Safety Hardening

- [x] **Status:** Done
- **Module/area:** `ci`, repository hygiene
- **User value:** No dead CI definitions that echo secret-derived values or contradict the real release process, and no committable secret files.
- **Release target:** `Not release-relevant`
- **Changelog impact:** `Fixed`
- **Acceptance criteria:**
  - [x] `modules/lib/.github/workflows/publish.yml` removed (never executed by GitHub Actions from a nested path, trigger deactivated, echoed `$OSSRH_USERNAME`, used JDK 17 against the repo's Java 21 standard).
  - [x] `.gitignore` excludes `.env*`, `*.pem`, `*.key`, `credentials*`, `.DS_Store`, and `.claude/settings.local.json`.
- **Validation:**
  - [x] `git status` shows the deletion and only intended changes; docs read-through.

## Work Log

| Date | Item | Update | Blockers/Decisions |
| --- | --- | --- | --- |
| 2026-07-01 | Item 1 | AI-first audit scored the repo across 7 dimensions (skill usage RED, structure/safety/workflow YELLOW); an independent review confirmed the scores. | Decision: skills reference existing docs (progressive disclosure) rather than duplicating them. |
| 2026-07-01 | Item 1 | Created `/sprint` and `/release-prep` skills, readiness script, and root `CLAUDE.md`; script validated on pass and fail paths. | None. |
| 2026-07-01 | Item 2 | Removed dead nested KuksoLib publish workflow and hardened `.gitignore`. | Decision: keep root `release.yml` KuksoLib Maven publishing untouched; whether that path is still needed is carried to the backlog. |
| 2026-07-02 | Item 2 | Maintainer decision: keep the `:lib` Maven publishing path in `release.yml` for possible future use (no Nexus server or lib user base today, so it stays gated behind `ENABLE_MAVEN_PUBLISH`). | Decision: no backlog item needed; revisit only if the lib gains external consumers. |

## Review

- **Completed items:** Item 1 (project skills + CLAUDE.md), Item 2 (safety hardening).
- **Verification:** `check-release-ready.sh` pass/fail runs, `git diff --check`, docs read-through.
- **Changelog status:** `CHANGELOG.md` updated under `Unreleased` with `repo` and `ci` entries and this sprint link.
- **Not completed:** None. Carryover: a `Ready` backlog item for seeding the empty `src/test/java` trees; the `:lib` Maven publishing path stays in `release.yml` for future use per maintainer decision (2026-07-02 work log).

## Retro

- The workflow docs were already prescriptive enough that skill extraction was mostly linking, not writing — a sign the documentation discipline is paying off.
