# Sprint 3: 2026-07-03

## Sprint Goal

Seed real unit test coverage across the plugin modules so `./gradlew test` catches parsing, validation, and command-utility regressions without a manual Paper server check.

## Product Alignment

- Maintenance sprint. It started with the safety net AGENTS.md's testing guidelines call for, then added documentation/default-config cleanup so public module contracts match the shipped plugin behavior `PRODUCT.md` expects.

## Dates

- **Start:** 2026-07-03
- **End:** 2026-07-10
- **Cadence:** 1 week

## Capacity And Assumptions

- Initial scope was test-only; later maintenance add-ons may touch documentation, default resources, and small mismatches discovered while aligning public contracts. No `version.txt` bumps by default.
- Tests must target real logic (parsing, validation, command utilities) that is unit-testable without a running Paper server; Bukkit-API-heavy code stays out of scope for this sprint.
- The repository currently has no `src/test` trees, so JUnit test dependencies may need to be added to the shared Gradle convention in `build-logic`.

## Committed Items

### Item 1: Seed Unit Test Trees Across Modules

- [x] **Status:** Done
- **Module/area:** All plugin modules (`:lib`, `:dialogs`, `:dialogs-exp-config-addon`, `:worlds`, `:items`), plus `build-logic` if test dependencies must be added to the shared convention.
- **User value:** Regressions in parsing, validation, and command utilities are caught by `./gradlew test` instead of manual Paper server checks.
- **Release target:** `Not release-relevant`
- **Changelog impact:** `Added`
- **Acceptance criteria:**
  - [x] Each module with testable parsing/validation/command-utility logic gets a seeded `modules/<module>/src/test/java` tree with at least one focused `*Test` class covering real behavior (no placeholder asserts).
  - [x] Modules with no unit-testable logic are explicitly noted in the work log instead of receiving empty or placeholder test trees.
  - [x] `./gradlew test` runs the new tests green.
- **Validation:**
  - [x] `./gradlew test` passes and reports the new test classes as executed.
  - [x] Review that the covered logic matches AGENTS.md testing guidelines (focused unit tests for parsing, validation, and command utilities).

### Item 2: Repository Hygiene Cleanup

- [x] **Status:** Done
- **Module/area:** Repository (`repo`); maintenance add-on beyond this sprint's main goal.
- **User value:** A single clear Apache 2.0 license for the whole repository, a tool-agnostic skills location shared with Codex, and consistent module metadata.
- **Release target:** `Not release-relevant`
- **Changelog impact:** `Added`, `Changed`, `Fixed`
- **Acceptance criteria:**
  - [x] Root `LICENSE` exists (moved from `modules/lib`); `LICENSE` is bundled as `META-INF/LICENSE` in all module jars and declared in the `:lib` Maven POM.
  - [x] Project skills relocated to `.agents/skills` with a `.claude/skills` symlink for Claude Code discovery.
  - [x] `modules/items/src/main/resources/plugin.yml` website/`load`/`description` fixed to match `modules/worlds` conventions.
  - [x] `modules/dialogs-exp-config-addon/README.md` added.
  - [x] Empty `gradle.properties` files and redundant per-module `.gitignore` files removed, with credential rules folded into the root `.gitignore`.
  - [x] Root `.gitattributes` added.
  - [x] `CHANGELOG.md` updated for the above.
- **Validation:**
  - [x] `./gradlew build` green.
  - [x] Built module jar contains `META-INF/LICENSE`.
  - [x] Skills remain invocable after the relocation.

### Item 3: Encoding Repair And Lib Documentation/Product Alignment

- [x] **Status:** Done
- **Module/area:** Repository (`repo`), `:lib`, `docs`; maintenance add-on from a lib blindspot audit.
- **User value:** Players stop seeing mojibake in plugin messages and Turkish localization; server owners get a lib README that matches implemented behavior; future work sessions get accurate product context (lib adoption direction, feature status, license) from `PRODUCT.md`.
- **Release target:** Next `:lib` release (encoding fix is user-visible).
- **Changelog impact:** `Changed`, `Fixed`
- **Acceptance criteria:**
  - [x] Double-encoded UTF-8 repaired across `modules/` sources (Java string literals/comments, `lang/tr.yml`, `items.yml`, module READMEs); no mojibake patterns remain outside `build/`.
  - [x] `modules/lib/README.md` rewritten to match reality: no auto-updater/REST/GUI presented as shipped, ProtocolLib requirement documented, consumer list corrected (worlds depends, items softdepends; dialogs not yet), config example synced, GUI scratch notes moved to the backlog.
  - [x] Root `LICENSE` replaced with an All Rights Reserved notice; `:lib` POM license declaration and `modules/items/README.md` license statement updated to match.
  - [x] Ghost softdepends (KuksoLoots, KuksoSense, KuksoConquest, KuksoDungeons) removed from `modules/lib/src/main/resources/plugin.yml` and captured as backlog items.
  - [x] `PRODUCT.md` records the lib adoption direction, a Shipped/Planned feature-status convention, doc audience rules, and the license.
  - [x] Backlog gains items for REST API, GUI API, auto-updater, async `VersionChecker`, the four planned plugins, and lib localization test coverage.
- **Validation:**
  - [x] Mojibake grep over `modules/` (excluding `build/`) returns nothing.
  - [x] `./gradlew build test` green.
  - [x] Read-through of `PRODUCT.md`, lib README, backlog, and `CHANGELOG.md` updates.

### Item 4: Module README And Default Config Public Contract Cleanup

- [x] **Status:** Done
- **Module/area:** All module READMEs, public default resources, source notes, `:dialogs` command namespace, `docs`
- **User value:** Server owners see module READMEs that describe what each module actually does, while future work stays in the continuous development cycle.
- **Release target:** Next affected module releases
- **Changelog impact:** `Changed`, `Fixed`, `Config And Permissions`
- **Acceptance criteria:**
  - [x] Each module README covers purpose, shipped features/utilities, installation, usage examples when available, API/addon examples when available, terms, and support.
  - [x] Future work, TODOs, roadmap notes, and scratch notes are removed from module READMEs, public default-resource comments, and source-level notes, then represented in `docs/sprints/backlog.md`.
  - [x] Stale default config entries are removed or aligned with implemented behavior.
  - [x] KuksoDialogs default config, examples, command registration, and user-facing usage strings use the shipped `/kuksodialogs` namespace.
  - [x] `PRODUCT.md` no longer permits planned-feature sections in module READMEs.
  - [x] Remaining unimplemented feature ideas stay in the backlog instead of default config or module READMEs.
- **Validation:**
  - [x] Module README/resource/source future-work grep is clean.
  - [x] Stale config-key and legacy namespace grep over `modules/` is clean.
  - [x] `git diff --check` passes.
  - [x] `./gradlew :lib:build :dialogs:build :worlds:build :items:build` passes.

## Work Log

| Date | Item | Update | Blockers/Decisions |
| --- | --- | --- | --- |
| 2026-07-03 | Item 1 | Sprint planned; item moved from backlog `Ready`. Implementation not started yet. | None. |
| 2026-07-03 | Item 1 | Added shared JUnit wiring and seeded meaningful unit tests for `:lib`, `:dialogs`, `:dialogs-exp-config-addon`, `:worlds`, and `:items`. | Minimal main-source changes were limited to exposing/extracting existing helper logic for testability: addon payload normalization, worlds config-key validation, and items legacy color formatting. |
| 2026-07-03 | Item 1 | `git diff --check` passed. A targeted `javac` compile sanity check passed for the changed main classes and new tests using cached dependency jars plus temporary JUnit API stubs. | Exact `./gradlew test` and `./gradlew build` could not start in this sandbox: the wrapper cannot write `/Users/burak/.gradle/.../gradle-9.2.0-bin.zip.lck`, and a writable-home Gradle attempt is blocked by local socket restrictions. |
| 2026-07-03 | Item 1 | `./gradlew test` rerun in a normal local shell: BUILD SUCCESSFUL, 16 tests executed across the five modules (lib 2, dialogs 6, dialogs-exp-config-addon 3, worlds 2, items 3), 0 failures. Item closed. | None. |
| 2026-07-03 | Item 2 | Implementation started. | None. |
| 2026-07-03 | Item 2 | Implemented and verified: `./gradlew build` green; all five module jars plus the `:lib` sources jar contain `META-INF/LICENSE`; generated `:lib` POM declares Apache-2.0; `.claude/skills` is a mode-120000 symlink to `../.agents/skills` and both skills resolve through it; items `plugin.yml` in the built jar carries the corrected website, `load: POSTWORLD`, and description. Item closed. | Renormalization skipped: no tracked file contained CRLF, so `.gitattributes` introduction is a no-op for existing content. Windows checkouts need `core.symlinks=true` (noted in backlog). |
| 2026-07-04 | Item 3 | Item added from a `:lib` blindspot audit: repo-wide mojibake, README claims of unimplemented features (auto-updater, REST, GUI), a false KuksoDialogs consumer claim, an Apache-2.0/All-Rights-Reserved license contradiction, and softdepends on four unstarted plugins. | User rulings: fix encoding repo-wide; keep lib focus on stability (logging, localization, color manager); backlog the wanted features; switch the repository to All Rights Reserved; backlog the planned plugins and clear their softdepends; record intent in `PRODUCT.md`. |
| 2026-07-04 | Item 3 | Encoding repair delegated to a task agent: 26 files fixed across `:lib`, `:worlds`, `:items`, `:dialogs` (cp1252/UTF-8 roundtrip plus targeted replacements for C1-control-byte sequences and smart quotes). `lang/tr.yml` Turkish diacritics and `VersionChecker` `§` codes verified restored. | `modules/lib/README.md` excluded from the automated pass because it was rewritten by hand in the same change. |
| 2026-07-04 | Item 3 | Docs and metadata updated by hand: root `LICENSE` replaced with All Rights Reserved (plus `:lib` POM and items README alignment), lib README rewritten, lib `plugin.yml` ghost softdepends removed, `PRODUCT.md` extended, six backlog items added, `CHANGELOG.md` updated. Mojibake grep clean; `./gradlew build test` green. Item closed. | GUI design scratch notes preserved verbatim in the backlog GUI item before removal from the README. |
| 2026-07-04 | Item 4 | Rewrote all module READMEs as shipped-behavior public docs, removed future-facing comments from public default resources and source notes, moved follow-up work into backlog, and updated `PRODUCT.md` so planned work belongs in backlog/sprints instead of module READMEs. | Remaining unimplemented feature ideas were backlogged instead of documented as shipped behavior. |
| 2026-07-04 | Item 4 | Removed stale default config entries: `:lib` dead REST flag and unregistered `log`/`gui` command config, `:items` unused debug key, `:worlds` unimplemented module toggles, and `:dialogs` unregistered `help`/`forceclose` command config. | KuksoDialogs namespace drift was fixed directly because default config, examples, plugin metadata, and command registration all need to agree on `/kuksodialogs`. |
| 2026-07-04 | Item 4 | Validation passed: module README/resource/source future-work grep clean, stale config-key and legacy namespace grep over `modules/` clean, `git diff --check` green, and `./gradlew :lib:build :dialogs:build :worlds:build :items:build` green. Item closed. | Targeted module builds were run after touching Java source and default resources. |

## Review

- **Completed items:** Item 1 — test source trees and focused tests added for all five targeted modules, with shared JUnit wiring in `build-logic`. Item 3 — repo-wide encoding repair plus lib documentation/product alignment and the All Rights Reserved license switch. Item 4 — all module READMEs rewritten as shipped-behavior public docs, stale default config removed, KuksoDialogs namespace defaults aligned, and future work routed to backlog.
- **Verification:** `git diff --check` passed. `./gradlew test` BUILD SUCCESSFUL with 16 tests executed (lib 2, dialogs 6, dialogs-exp-config-addon 3, worlds 2, items 3), 0 failures. Item 3: mojibake grep over `modules/` clean and `./gradlew build test` green after the repair. Item 4: module README/resource/source future-work grep clean, stale config-key and legacy namespace grep over `modules/` clean, `git diff --check` green, and `./gradlew :lib:build :dialogs:build :worlds:build :items:build` green.
- **Changelog status:** Items 1-2 not release-relevant. Item 3 recorded under `[Unreleased]` in `CHANGELOG.md` (encoding fix is user-visible; license and lib doc changes are release-relevant). Item 4 recorded under `[Unreleased]` as docs, config, and permission cleanup.
- **Not completed:** None.

## Retro

- **Keep:** Delegating well-scoped, doc-backed sprint items to a coding agent worked; the sprint file's acceptance criteria were specific enough to implement without clarification.
- **Change:** Agent sandboxes may block Gradle daemon startup; plan for a local verification pass after delegated implementation work.
- **Follow up:** Extracted helpers (`ExpConfigValues`, worlds config-key validation, items color formatting) are natural seams for future test growth.

## Carryover

- [x] Rerun `./gradlew test` and `./gradlew build` in an environment that can write Gradle wrapper locks and open Gradle's local daemon/cache sockets. Done 2026-07-03: `./gradlew test` green with 16 tests.
