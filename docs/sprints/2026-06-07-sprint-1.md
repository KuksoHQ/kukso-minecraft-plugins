# Sprint 1: 2026-06-07

Historical sprint converted from `docs/implementation-history/2026-06-07-kukso-worlds-localization-fix.md`.

## Sprint Goal

Fix KuksoWorlds localization and list-command behavior by integrating KuksoWorlds language loading with KuksoLib, registering the shared API provider correctly, and supporting the placeholder styles used across the plugin family.

## Product Alignment

- Supports `PRODUCT.md` principles for consistent localization, low-friction admin behavior, and reuse of `:lib` as the shared foundation for localization, placeholders, hooks, commands, and cross-plugin services.

## Dates

- **Start:** 2026-06-07
- **End:** 2026-06-07
- **Cadence:** 1 week

## Capacity And Assumptions

- Historical one-day implementation record converted into the current sprint format after the work was already completed.
- No release tag is known from the legacy note, so release target remains `Unreleased`.
- KuksoWorlds depends on the updated KuksoLib behavior and should be deployed with the matching KuksoLib jar.

## Committed Items

### Item 1: Fix KuksoWorlds Localization Integration

- [x] **Status:** Done
- **Module/area:** `:lib`, `:worlds`
- **User value:** Server admins receive translated `kw list` output and resolved placeholders instead of fallback API errors or raw placeholder tokens.
- **Release target:** `Unreleased`
- **Changelog impact:** `Fixed`, `Changed`
- **Acceptance criteria:**
  - [x] `kw list` no longer returns the `KuksoLib API not available` fallback when KuksoLib is present.
  - [x] KuksoWorlds can merge its own `lang/*.yml` files into the shared translation map without wiping KuksoLib translations.
  - [x] Console-only KuksoWorlds command dispatch translations use the correct key and locale argument order.
  - [x] Placeholder resolution supports both KuksoLib `{key}` tokens and KuksoWorlds `<key>` tokens, including `<name>` and `<status>`.
  - [x] Deployment expectation is documented: updated KuksoLib and KuksoWorlds jars should be deployed together.
- **Validation:**
  - [x] `./gradlew.bat build`
  - [x] Review implementation notes for provider registration, merged language loading, argument-order fixes, and placeholder support.

### Item 2: Migrate Legacy Implementation Note

- [x] **Status:** Done
- **Module/area:** `docs`
- **User value:** Historical work is traceable through the current sprint workflow instead of remaining in legacy implementation-history records.
- **Release target:** `Not release-relevant`
- **Changelog impact:** `Changed`
- **Acceptance criteria:**
  - [x] The legacy 2026-06-07 implementation-history note is represented in this sprint file.
  - [x] The old legacy note is removed to avoid duplicate records.
  - [x] `docs/sprints/backlog.md` records the migration as completed.
- **Validation:**
  - [x] Sprint document read-through confirms the legacy plan, implementation, verification, and notes are preserved.
  - [x] `git diff --check -- docs/sprints CHANGELOG.md`

## Work Log

| Date | Item | Update | Blockers/Decisions |
| --- | --- | --- | --- |
| 2026-06-07 | Item 1 | Planned fix for `kw list` returning `KuksoLib API not available` instead of translated `commands.list.*` messages, followed by unresolved `<name>` and `<status>` placeholders after translations began loading. | None recorded. |
| 2026-06-07 | Item 1 | Registered the static `KuksoAPIProvider` during KuksoLib service startup and cleared it on shutdown. | KuksoWorlds needed the shared provider registration from the updated Lib module. |
| 2026-06-07 | Item 1 | Added `Lang.loadAdditional(Plugin)` so dependent plugins can merge their own `lang/*.yml` files into the shared translation map without wiping KuksoLib translations. | Preserve KuksoLib translations while loading dependent plugin language files. |
| 2026-06-07 | Item 1 | Updated KuksoWorlds to save and merge-load its language files after KuksoLib is available. | KuksoWorlds language loading now depends on the shared KuksoLib lifecycle. |
| 2026-06-07 | Item 1 | Corrected two console-only `Lang.t` calls in KuksoWorlds command dispatch where key and locale arguments were reversed. | None recorded. |
| 2026-06-07 | Item 1 | Updated `PlaceholderManager` to resolve both `{key}` and `<key>` token styles. | KuksoLib messages already use `{key}`, while KuksoWorlds language files use `<key>`. |
| 2026-06-30 | Item 2 | Converted the legacy implementation-history record into the current sprint format and removed the old note to avoid duplicate historical records. | This is a historical migration, not new plugin behavior. |

## Review

- **Completed items:** KuksoLib provider registration, additional language merge loading, KuksoWorlds language integration, corrected `Lang.t` argument order in KuksoWorlds command dispatch, dual-style placeholder replacement, and migration of the legacy note into this sprint file.
- **Verification:** Legacy implementation verification recorded `./gradlew.bat build`; build passed after both fixes. Migration verification included sprint document read-through and `git diff --check -- docs/sprints CHANGELOG.md`.
- **Changelog status:** `CHANGELOG.md` records the docs migration under `Unreleased`. Plugin behavior remains targeted to `Unreleased` because no release tag was known from the legacy note.
- **Not completed:** No incomplete items recorded.

## Retro

- **Keep:** Shared localization and placeholder behavior in `:lib` so feature plugins stay consistent.
- **Change:** Record implementation work directly in `docs/sprints/` instead of adding new legacy implementation-history notes.
- **Follow up:** Deploy updated KuksoLib and KuksoWorlds jars together; KuksoWorlds expects the provider registration, merged language loading behavior, and `<key>` placeholder support from the updated Lib module.

## Carryover

- [ ] Deploy updated KuksoLib and KuksoWorlds jars together.
